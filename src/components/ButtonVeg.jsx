import { Button } from "@chakra-ui/react";
export const ButtonVeg = ({ onClick, healthLabels }) => {
  return (
    <>
      <Button
        onClick={() => {
          vegetarianDish;
        }}
      >
        Vegetarian
      </Button>
    </>
  );
};
